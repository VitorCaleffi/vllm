# Common dependencies
-r common.txt

# NOTE: torch, torchvision, torchaudio, triton, and amdsmi are built from
# source in the Docker build (see docker/Dockerfile.rocm_base and
# docker/Dockerfile.rocm.strix-halo). They are NOT installed from PyPI
# or external wheel indexes during Docker builds.
#
# This file is only used for local development builds where you already
# have a compatible ROCm + PyTorch environment set up on your host.
cmake>=3.26.1,<4
packaging>=24.2
setuptools>=77.0.3,<80.0.0
setuptools-scm>=8
wheel
jinja2>=3.1.6
timm>=1.0.17
